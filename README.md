# dantiberi.com Front-End

Personal professional site for Dan Tiberi

## Quick start

```bash
npm install --legacy-peer-deps
ng serve
# open http://localhost:4200
```

- `ng build` (or `--configuration production`)
- `ng test`
